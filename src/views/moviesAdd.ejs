<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie add</title>
    <link rel='stylesheet' href='/css/style.css' />
</head>

<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
      </figure>
    <h1 class="titulo">Agregar Película</h1>
    <br>
    <section class="formulario">
        <form action="/movies/create" method="POST">
            <div>
                <label for="title">Title </label>
                <p><input type="text" name="title" id="" value="<%= locals.old && (old.title && old.title) %>"></p>
                <spam class="ms-2 fw-bold"><%= locals.errors && (errors.title && errors.title.msg) %></spam>
            </div>
            <div>
                <label for="rating">Rating </label>
                <p><input type="number" name="rating" id="" value="<%= locals.old && (old.rating && old.rating) %>"></p>
                <spam class="ms-2 fw-bold"><%= locals.errors && (errors.rating && errors.rating.msg) %></spam>
            </div>
            <div>
                <label for="awards">Awards </label>
                <p><input type="number" name="awards" id="" value="<%= locals.old && (old.awards && old.awards) %>"></p>
                <spam class="ms-2 fw-bold"><%= locals.errors && (errors.awards && errors.awards.msg) %></spam>
            </div>
            <div>
                <label for="release_date">Release date </label>
                <p><input type="date" name="release_date" id="" value="<%= locals.old && (old.release_date && old.release_date) %>"></p>
                <spam class="ms-2 fw-bold"><%= locals.errors && (errors.release_date && errors.release_date.msg) %></spam>
            </div>
            <div>
                <label for="length">Length </label>
                <p><input type="number" name="length" id="" value="<%= locals.old && (old.length && old.length) %>"></p>
                <spam class="ms-2 fw-bold"><%= locals.errors && (errors.length && errors.length.msg) %></spam>
            </div>
            <br>
            <p> <button class="botonAgregar" type="submit">Agregar</button>
            
            <a  class="botonVolver" href="/movies">Listado de Películas</a></p>
        </form>
    </section>
    
</body>

</html>